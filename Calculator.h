template < typename A>
A fadd(A x, A y)
{
   return x+y;
}

template < typename A>
A fsub(A x, A y)
{
   return x-y;
}

template < typename A>
A fmul(A x, A y)
{
   return x*y;
}

template < typename A>
A fdiv(A x, A y)
{
   return x/y;
}
